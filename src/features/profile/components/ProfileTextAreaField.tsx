import FormTextAreaField, {
  type FormTextAreaFieldProps,
} from "@/shared/ui/FormTextAreaField";

export default function ProfileTextAreaField(props: FormTextAreaFieldProps) {
  return <FormTextAreaField {...props} />;
}
